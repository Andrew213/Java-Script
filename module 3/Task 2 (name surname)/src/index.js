let name = 'еГоР';
let surname = 'иВаНоВ';

let rightName = name.substr(0, 1).toUpperCase() + name.substr(1).toLowerCase();
let rightSurname = surname.substr(0, 1).toUpperCase() + surname.substr(1).toLowerCase();

alert(rightName + ' ' + rightSurname)